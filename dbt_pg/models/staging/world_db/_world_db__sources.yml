version: 2

sources:
  - name: world_db #must be name of the db also because we are ref it in models like this: source('pg_db', 'test')
    schema: public
    database: pg_db
    tables:
      - name: city
      - name: country
      - name: country_language